#!/bin/sh

function start {
    local compose_path=$(find ../.repos/ -type f -name "docker-compose.yml" -printf ' -f %p')
    local compose_args=${@}
    local cmd="docker-compose $compose_path $COMPOSE_PATH_OVERRIDE $compose_args"
    echo $cmd
    exec $cmd
}
